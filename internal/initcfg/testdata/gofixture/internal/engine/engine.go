// Package engine is the outermost layer of the gofixture test project.
package engine

// Run is a placeholder entrypoint so the package has a public symbol to import.
func Run() string { return "engine" }
