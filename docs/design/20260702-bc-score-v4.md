# bc_score.v4 — strength & distance classification fixes (Wave 4)

Status: SHIPPED (branch `fix/wave1-gate-integrity`, PR #25, wave 4 of the
2026-07-02 eval remediation; commits `25d4e4b` const/var, `1cb538b` DTO,
`e64b77d` D=10). Full engine design: `bc-measurement-v4.md`. Formula
unchanged — still Khononov Ch10
verbatim: `balance = max(|S−D|, 10−V) + 1`. What changes in v4 is the
classification feeding the formula. Ordinal assignment changes are a
breaking metric change (CLAUDE.md), so the version bumps once, up front,
and **v4 scores are not comparable to v3 scores**.

Source findings: `docs/archived/reports/eval-2026-07-02-v1.1.2/00-FINDINGS.md` §1
(deviations 1–3) and `docs/archived/reports/eval-2026-07-02-v1.1.2/book-conformance.md`.

## The three classification changes v4 covers

1. **Go const/var reads score Model (S=3), not Functional (S=8).** _(shipped)_
   Referencing another module's exported const/var is pure data sharing —
   the book's Model coupling (Ch7). The Go extractor mapped the resolved
   object kinds `const`/`var` to `functional`, a 5-point error on a
   10-point ordinal scale, unfixable by config. Funcs stay `functional`,
   interfaces stay `contract`, concrete types stay `model`.

2. **Pure-data DTOs reach Contract (S=1) in Go.** _(shipped)_ An exported
   struct with at least one field, every field exported, no func-/chan-typed
   fields (behavior carriers), and an empty method set (value + pointer
   receivers, promoted methods included; zero-field marker structs are NOT
   DTOs), referenced across a config-declared `public:` glob boundary,
   upgrades the public-glob floor from not-intrusive to `contract` — the
   book's canonical Contract example is exactly a concrete integration DTO.
   The extractor emits a context-dependent hint `dto`
   (`graph.StrengthHintDTO`, rank between contract and model); fields of a
   qualifying DTO carry the same hint so composite-literal keys and selector
   reads don't outrank the type reference away. Anything with methods or
   unexported fields stays `model`; a DTO not crossing a declared public
   boundary stays `model` (the boundary declaration is what makes it a
   contract); an internal-glob match stays authoritative intrusive.
   TS/Py/Rust have no equivalent static signal and abstain — SCIP indexes
   carry no method-set/field-visibility information (documented in
   `scip_reader_test.py`), grimp has no object kinds, dependency-cruiser has
   no type shapes; Wave 7's LLM labels are the designed path there.

3. **D=10 rung for declared external systems.** _(shipped)_ New frozen ordinal
   `DistanceExternal = 10` (book Ch10 Example 1, cross-vendor integration).
   Edges matching a config-declared `external_systems:` entry enter scoring
   at D=10 (default volatility low, per the book's generic-subdomain
   guidance). Undeclared external edges keep the disclosed exclusion
   (`classified_edges.external`) — scoring every library import at D=10
   would flood the metric with vendor noise; the book's example is a
   _declared integration seam_.

## Consumer impact

- `score_version` now appears at the top level of `--json` output (always
  present), in addition to per-finding `matched_by.score_version` on BC
  advisories. Consumers key on it; treat a version change as a new
  measurement, not a delta.
- Baselines storing a v3 `score` snapshot must be re-baselined before
  `coupling.gate.max_drop` comparisons are meaningful again (Wave 4 Task 5).
- Attribution: `make corpus-attrib` prints the coupling_balance
  score/band/scored/abstained/external row per attribution repo (archfit
  Go, ccgram Python, herdr Rust, storybook TS) for the before/after tables
  in `docs/plans/20260702-wave4-book-strength-distance.md`.

## Changelog entry (curated tag message for the next release)

Releases are tag-triggered; the annotated tag message IS the release notes
(`release.yaml`). Include this block in the next tag:

```
Breaking metric change: ScoreVersion is now bc_score.v4. v4 scores are NOT
comparable to bc_score.v3 scores — treat the first v4 run as a new
measurement, and re-baseline (`archfit baseline`) before relying on
`coupling.gate.max_drop`. `score_version` now appears at the top level of
--json output so consumers can key on it.

Three classification changes (formula unchanged, still book-verbatim):

- const/var reads score Model (S=3), not Functional (S=8): Go resolved
  object kinds const/var, and rust-analyzer SCIP const/static/field terms —
  pure data sharing per the book's Ch7. TS/Py are unchanged (documented
  abstentions).
- Pure-data Go DTOs reach Contract (S=1): an exported struct with only
  exported data fields and an empty method set, referenced across a declared
  `public:` glob boundary, upgrades the public-glob floor to contract.
  TS/Py/Rust abstain (no static signal).
- New D=10 distance rung for declared external systems: config
  `external_systems:` entries (target globs + optional volatility, default
  low) enter scoring at DistanceExternal=10 per the book's Ch10 Example 1;
  undeclared external edges keep the disclosed exclusion
  (`classified_edges.external`), with the declared count reported in
  `classified_edges.declared_external`.
```
