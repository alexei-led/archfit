# book-alignment cleanup — execution progress

Plan: docs/plans/20260629-archfit-book-alignment-cleanup.md
Branch: feat/analyze-cli · baseline build green, tree clean.

Orchestration: Phase 0 + Phase 8 = parallel read-only workflows. Phases 1–7 =
sequential single mutator each, reviewed + gated + committed per phase.

## Status

- [x] Phase 0 — verify dependency map + drifted edit-target line numbers (WORKFLOW, read-only)
- [x] Phase 1 — scorecard reduction (D1) — committed 44b6d11 (golden 92/strong->50/mixed, expected)
- [x] Phase 2 — remove cut metrics — committed 91b92f8 (-6830 LOC, grep clean)
- [x] Phase 2b — remove cut rules + config refs (ATOMIC) — committed a8f89d8 (+ dc9e76a P2 test fixup 27->5)
- [x] Phase 3 — remove orphaned signals + extractors + pipeline wiring — committed 4e6d42e (-3669 LOC; kept clones/scip/loc + AsDuplication trimmed)
- [ ] Phase 3b — trim orphaned ast-grep syntax kinds (task #24, AFTER core; trace SyntaxFacts consumer first)
- [x] Phase 4 — drop gocyclo + lizard — committed 08c6172 (jscpd kept; regen'd 2 testdata baselines 27->5 + schema; CAUGHT agent's bogus 27-metric baseline)
- [ ] Phase 5 — config / self-config / examples
- [ ] Phase 6 — docs / skills / spec / CLAUDE.md
- [ ] Phase 7 — regenerate schema + goldens + baselines (EYEBALL diffs)
- [ ] Phase 8 — full verification + no-debt orphan grep (WORKFLOW, read-only)

## RESOLVED DECISIONS (user, 2026-06-29)

- **Clones: KEEP clones + jscpd** (preserve StrengthSymmetric S=9 upgrade to
  coupling_balance). Consequences vs original plan:
  - Phase 3: do NOT delete internal/extract/clones. KEEP the Duplication signal
    type/field (engine.go:164-165 reads it). Still CUT the functional_candidates
    metric calculator; if its `AsDuplication()` accessor method becomes unused after
    that, remove the method but KEEP the Duplication struct/field + clones extractor.
  - Phase 4: drop ONLY gocyclo + lizard. jscpd STAYS. noteToolErr("jscpd")@259 stays.
  - Phase 5: KEEP analyzers.clones config key. Remove only analyzers.complexity.
    testdata configs: remove only the `complexity:` key, KEEP `clones:`.
  - .archfit.yaml: KEEP internal/extract/clones module decl; remove only
    internal/extract/complexity module decl (~321-329).
  - CLAUDE.md "Symmetric from clones" invariant STAYS unchanged.
- **coverage metric: KEEP** (report-only, no orphan). No action.

## Verified edit targets (Phase 0 — corrected line numbers)

### Phase 1 — scorecard (slice: collapse-to-coupling-balance-only)

- internal/score/score.go: `dims` slice @103-110 (6 entries -> keep only couplingBalance);
  Dim* consts @56-62 (remove DimBoundaryIntegrity/DependencyGraphHealth/CohesionModularity/
  ChangeLocality/ArchitectureFitness/AnalysisConfidence; KEEP DimCouplingBalance@57);
  META MACHINERY also goes: analysisConfidence append @133-138, finalizeMeta() @167,
  naDimensionPenalty const @178, structuralDimensions map @184-189, naStructural logic;
  meanValue(dims) @354 + Scorecard.Overall @87 -> couplingBalance.Value; pkg doc @1-4 stale.
- internal/score/score_boundary_coupling.go: remove boundaryIntegrity() @28; KEEP couplingBalance() @113.
- internal/score/score_cohesion_health.go: remove dependencyGraphHealth()@43, cohesionModularity()@112,
  changeLocality()@173, architectureFitness()@213, analysisConfidence()@242 — LIKELY DELETE WHOLE FILE
  (also semanticTools={scip,lizard,jscpd} @29 goes with it — verify file empties).
- internal/decision/decision.go: whatMovesTable @177-183 (drop 5 removed dims, KEEP DimCouplingBalance@179);
  Report.Overall comment @41 stale; decideBand@134 + sc.OverallBand reads @121,144,148 STAY.
- cmd/archfit/llmreview.go: validDimNames @228-236 (drop 6, keep DimCouplingBalance@230);
  buildReviewPrompt sc.Dimensions iter @557-564 (works, 1 dim); reviewSystemPrompt "at most 7 dimensions" @178 stale.
- TESTS (must compile+pass in Phase 1): score_test.go @118-120,129,187,321,340,345,449,502,507,570,617,1012,1021;
  decision_test.go @56-62,129,153,235,457,470; scorecard_test.go golden @68-100 + dim-list assert @225-227
  (golden = deliberate regen, eyeball).

### Phase 2 — metrics (DELETE files vs KEEP-symbols-in-shared-files)

- coreRingPkgs in internal/arch_test.go: var @20; risk@27, intramodule@28, result@29. Drop risk+intramodule lines.
- DELETE: internal/metrics/risk (risk_hub). intramodule = ComplexityMetric(cut)+ArchitectureFitnessMetric
  (architecture_fitness IS cut — reviewer missed; package deletes cleanly). martin.go (all 3 Martin + helpers).
  propagation.go (grep ComputePropagation first). cohesion_lcom calculator.
- MUST STAY (shared infra — do NOT delete): internal/metrics/internal/modgraph (blast_radius uses it),
  internal/metrics/internal/result (registry infra), internal/metrics/modularity/constants.go KEEP ONLY
  `hubBlastThreshold`@9 (blast_radius.go:49 uses it) — remove the other dead consts.
- UNLISTED: change_locality (IS cut, composites), coverage (line 64 — OPEN DECISION above). Keep registry/adapt.

### Phase 2b — rules (ATOMIC, one commit)

- internal/rules/rules.go: switch cases for struct_field_max/test_in_production/layer_role_divergence +
  defaultGateForType @137-138 SHARED 5-way case — SPLIT carefully, preserve public_api_change/public_api_type_leak.
- impls: structFieldMax, layerRoleDivergence (rules.go), testInProduction (rules_syntax.go) + rule tests.
- .archfit.yaml: only arch_struct_field_max @599-602 (no other removed-type entries). Same commit.
- stale (Phase 6/7, not compile-breaking): scripts/eval/coverage/main.go:117, archfit.schema.json:458,
  engine.go:115 comment, internal/config/modules.go:59 comment, internal/syntax/{fileclass,testfile}.go docs.

### Phase 3 — signals + extractors + wiring (line numbers EXACT, no drift)

- signal.go: remove AsHistory/AsComplexity/AsDuplication/AsFitness; KEEP AsSymbol/AsSize.
- DELETE pkgs: internal/extract/complexity, internal/extract/clones (PENDING clones decision), internal/fitness.
- internal/history/git/git.go: remove History()+Churn()@132 (Churn calls History, trivial); KEEP RepoRoot/HeadRef/Changed.
- cmd/archfit/pipeline_run.go wiring: git.History@166, fitness.Detect@181, complexity.Run@245, clones.Run@258 (EXACT).
- EXECUTE (non-blocking, advisor): also remove History.CoChange consumer in facts.Build @engine.go:289
  (cutting churn reader empties co-change file-fact — drop it same commit). And engine.go:164-165
  Duplication.Clusters->CrossModuleClonePairs removal is PART of the clones decision.

### Phase 4/5 — tools + config

- registry.go CLEAN (no tool refs). pipeline_run.go comments @234,251 + noteToolErr("jscpd")@259 cleanup (Phase 3).
- pipeline_coverage.go:64 gocyclo install hint in toolAstGrep entry — remove gocyclo mention.
- NO analyzers.churn key (git is in-process; views.go:134-135). Remove analyzers.complexity/clones struct fields.
- MUST UPDATE (else strict-decode breaks TESTS): internal/extract/golang/testdata/single-module/.archfit.yaml
  @31-33 + one-member-workspace/.archfit.yaml @31-33 (complexity:/clones: keys) — same commit as struct-field removal.
- MUST REMOVE (else dogfood gate breaks): .archfit.yaml @311-329 module decls for internal/extract/clones +
  internal/extract/complexity.
- OUT OF no-debt-grep scope (reports/eval/configs/ ~5 + reports/eval-2026-06-28/ideal-configs/ ~8): stale eval
  artifacts, not test-loaded; flag to user, low priority.

### Deferred orphans surfaced during Phase 2b (track to Phase 8 grep)

- internal/extract/astgrep/rules/go.yml:267 struct-field-count ast-grep rule + any
  SizeSignals struct-field fact: orphaned after struct_field_density (P2) +
  struct_field_max (P2b) removal. HANDLE in Phase 3 (orphaned facts).
- internal/syntax/testfile.go test-framework-import helper: was used by test_in_production.
  FileClass Test classification survives (loc walk). Check if the framework-import helper
  is now orphaned. HANDLE Phase 3. Doc comments fileclass.go:4 + testfile.go:9 → Phase 6.
- internal/config/modules.go:59 Threshold field: only consumer was layer_role_divergence.
  KEEP field (back-compat, strict-decode) but update comment. archfit.schema.json:458
  description regen via `make schema` in Phase 7.
- internal/engine/engine.go:115 comment: fixed in Phase 2b (drop struct_field_max/test_in_production).
- docs/guide/{metrics,configuration-reference}.md + CLAUDE.md + docs/design: Phase 6.
- reports/ eval configs (2 files w/ removed rule types): OUT of no-debt scope, low priority.

### Phase 8 — add to orphan grep: `file_class|FileClass` (verify FileClass survives via loc walk empirically).
